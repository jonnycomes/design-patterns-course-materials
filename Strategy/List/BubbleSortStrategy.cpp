#include "BubbleSortStrategy.h"
#include <vector>
#include <iostream>

void BubbleSortStrategy::sort(std::vector<int>& numbers) {
  std::cout << "----------------------------------------------------------\n"
            << "Bubble sort is not yet implemented...                     \n"
            << "Instead, bubble sort just adds a 0 to the end of your list\n"
            << "----------------------------------------------------------\n";
  numbers.push_back(0);
}