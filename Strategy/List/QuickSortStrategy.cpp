#include "QuickSortStrategy.h"
#include <vector>
#include <iostream>

void QuickSortStrategy::sort(std::vector<int>& numbers) {
  std::cout << "----------------------------------------------------------\n"
            << "Quick sort is not yet implemented...                      \n"
            << "Instead, quick sort just adds a 1 to the end of your list \n"
            << "----------------------------------------------------------\n";
  numbers.push_back(1);
}